const mdLinks = require('../index.js');

describe(mdLinks, () => {

  it('Debería ingresar un argumento', () => {
    console.log('FIX ME!');
  });
  it('Debería emitir un error si la ruta no es valida', () => {
    console.log('FIX ME!');
  });
});
